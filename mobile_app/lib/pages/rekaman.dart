import 'package:flutter/material.dart';

class MyRekaman extends StatelessWidget {
  const MyRekaman({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Colors.red,
    );
  }
}