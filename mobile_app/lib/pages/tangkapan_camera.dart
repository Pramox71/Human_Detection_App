import 'package:flutter/material.dart';

class MyCapture extends StatelessWidget {
  const MyCapture({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Colors.green,
    );
  }
}